version: 2

models:
  - name: stg_fct_orders
    description: "Orders Staging table"

    columns:
      - name: ORDER_ID_KEY
        description: "order identifier"
        data_tests:
          - not_null